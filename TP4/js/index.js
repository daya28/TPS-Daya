/*(document).ready(function() {
 $.getJSON( “/paises.json”, function( data ) {

 })
 
============================================================
 $(document).on('ready',function (){
 		
		$.getJSON('paises.json', function(data) {
			$.each(data, function(key, value) {
				$("#paises").append('<option name="' + key + '">' + value + '</option>');
			}); // close each()
		}); // close getJSON()

	});

*/

